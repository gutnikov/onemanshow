# Shape only. Copy to staging.yaml and encrypt with sops before adding values.
POSTGRES_PASSWORD: ''
DATABASE_URL: ''
# Same database reached through the CI ssh tunnel, so the hooks stay the interface.
DATABASE_URL_TUNNEL: ''
REGISTRY_TOKEN_RO: ''
SENTRY_DSN: ''
