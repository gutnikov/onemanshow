# Shape only. Copy to staging.yaml and encrypt with sops before adding values.
#
# Deliberately without DATABASE_URL. The stand's database is a branch created
# per validation and its address comes from the provider's API at that moment, so
# a stored value here is stale by construction - and `verify secrets` warns about
# exactly that if it finds one.
BETTER_AUTH_SECRET: ''
REGISTRY_TOKEN_RO: ''
SENTRY_DSN: ''
