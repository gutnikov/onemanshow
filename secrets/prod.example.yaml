# Shape only. Copy to prod.yaml and encrypt with sops before adding values.
#
# What the container is given. Anything the pipeline needs but the application
# does not belongs in ci.example.yaml instead.
DATABASE_URL: ''
BETTER_AUTH_SECRET: ''
REGISTRY_TOKEN_RO: ''
SENTRY_DSN: ''
