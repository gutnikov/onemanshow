# References, not values. Kamal resolves each from the environment of the
# process that invokes it, and that process gets them from sops - so nothing
# secret is committed, and CI needs no separate wiring.
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
DATABASE_URL=$DATABASE_URL
SENTRY_DSN=$SENTRY_DSN
